version: "2"
sql:
  - engine: "sqlite"
    schema: "migrations"
    queries: "queries"
    gen:
      go:
        package: "db"
        out: "internal/store/db"
        sql_package: "database/sql"
        emit_json_tags: true
